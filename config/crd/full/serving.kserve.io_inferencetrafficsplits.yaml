---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferencetrafficsplits.serving.kserve.io
spec:
  group: serving.kserve.io
  names:
    kind: InferenceTrafficSplit
    listKind: InferenceTrafficSplitList
    plural: inferencetrafficsplits
    shortNames:
    - its
    singular: inferencetrafficsplit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InferenceTrafficSplit defines a weighted traffic split across multiple InferenceServices
          sharing a single ingress endpoint via Gateway API HTTPRoute.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceTrafficSplitSpec defines a weighted traffic split
              across multiple InferenceServices.
            properties:
              backends:
                description: Backends is the list of InferenceService references with
                  traffic weights.
                items:
                  description: TrafficSplitBackend references an InferenceService
                    and assigns a traffic weight.
                  properties:
                    inferenceServiceRef:
                      description: InferenceServiceRef is the name of the InferenceService
                        in the same namespace.
                      type: string
                    weight:
                      description: |-
                        Weight is the traffic weight for this backend. Gateway API normalizes weights
                        across all backends, so values do not need to sum to 100.
                      format: int32
                      type: integer
                  required:
                  - inferenceServiceRef
                  - weight
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - inferenceServiceRef
                x-kubernetes-list-type: map
            required:
            - backends
            type: object
          status:
            description: InferenceTrafficSplitStatus defines the observed state of
              InferenceTrafficSplit.
            properties:
              address:
                description: Address is the cluster-local address.
                properties:
                  CACerts:
                    description: |-
                      CACerts is the Certification Authority (CA) certificates in PEM format
                      according to https://www.rfc-editor.org/rfc/rfc7468.
                    type: string
                  audience:
                    description: Audience is the OIDC audience for this address.
                    type: string
                  name:
                    description: Name is the name of the address.
                    type: string
                  url:
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the InferenceTrafficSplit's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              url:
                description: URL is the shared endpoint for this traffic split.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
